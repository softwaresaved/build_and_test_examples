language: python

python:
  - "2.7"

services:
  - docker

before_install:
  - docker build -t hello -f Dockerfile .

script:
  - docker run hello -d
  - docker exec -it hello pylint hello.py
  - docker exec -it hello pytest tests/tests.py